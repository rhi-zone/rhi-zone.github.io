# skill-tiers.txt — per-skill distribution scope (two-tier).
#
# Format:  <skill-name>  <tier>
#   <skill-name> matches an entry in github-io's committed .claude/skills/
#                (directory <name>/ with a SKILL.md, plus optional siblings).
#   <tier> is one of:
#     all  — distribute to EVERY repo in skill-recipients.txt
#     dev  — distribute ONLY to repos in skill-recipients-rhizone.txt
#            (rhi-zone developer substrate)
#
# Comments (#) and blank lines ignored. A skill present in .claude/skills/ but
# absent from this file is NOT distributed (it stays hub-only) — fail-closed.

handoff                            all
polish                             all
survey-open-threads                all
think-with-the-engineering-taste   all

domain-model                       dev
improve-codebase-architecture      dev
