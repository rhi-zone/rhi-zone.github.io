# Private-name hash list — SAFE TO COMMIT.
#
# Each line is a SHA-256 hex digest of a protected name, NORMALIZED as:
# lowercase, then strip everything except [a-z0-9] (so "Acme-Widgets-Parents"
# and "acme widgets parents" and "ACME_WIDGETS_PARENTS" all normalize to the
# same string before hashing). No casing/spacing variants need separate
# entries — the checker applies the identical normalization to candidate
# text before hashing, so one hash per name covers all of those.
#
# This file intentionally contains ONLY hashes, never the plaintext names.
# Checked with: tooling/check-private-name-hashes.js (see that file for the
# tokenization + substring-matching approach, and for how to add a new
# name's hash without ever writing the plaintext into this repo).
#
# Paired with (but independent of) the local-only, untracked
# .git/info/private-names plaintext denylist used by .githooks/pre-commit.
# That file still doesn't travel with clones; this one does.


