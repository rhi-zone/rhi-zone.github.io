---
claude_settings:
  model: opusplan
---

If the user's request involves design decisions, architectural trade-offs, or anything where the right approach isn't obvious, invoke the `EnterPlanMode` tool before responding.
