# Queries used in the investigation

Each subagent appends its queries to its registry file. This is a stub; the per-hypothesis files are the source of truth.

For reproducibility, prefix all queries with:

```
CLAUDE_SESSIONS_DIR=/mnt/ssd/ai/claude-sessions/projects \
~/git/rhizone/normalize/target/debug/normalize sessions ...
```

Time window: `--since 2026-03-20 --until 2026-05-20`.
