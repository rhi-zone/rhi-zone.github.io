# Daily Log: 2026-07-29

**Continuation of theory work.** Fractal advanced the multi-round dialogue on fact attribution with a deeper second-order critique. Normalize, mirrorer, and other projects showed light continuation activity.

## Sessions and Activity

### fractal

**Session 9574ee84** (continuation from 2026-07-28, extending to 04:11+ UTC on 07-29) — Information-theoretic fact attribution theory, Round 2.

**Vellum's second pass:** After Fable's concessions on three key points, verified whether the remaining proposed structures hold:

1. **Telescoping identity verification**: Built controlled tests with incomparable overlaps (diamonds). Result: **total is order-free, attribution is order-dependent**. Per-fact credit swings by exactly the interaction term (I = N·log₂|U∩(A∧B)|/(|A||B|)) depending on scan order. Chains are order-free only under declared canon (not automatic). **Consequence: Crossing overlap cannot be separated from refinement overlap—both exhibit order-dependence.**

2. **FORSIED unification critique**: MaxEnt background-update architecture is clean, but boundary constraints (Fractal's dominant fact form: "field is always a string") break existence guarantees. FORSIED's interestingness ratio reintroduces description-length cost and ratio form—both retracted principles. **Net: Unification is architectural analogy only, not guarantee inheritance.**

3. **E-values as unifier critique**: LR construction valid only under simple/split nulls (Wasserman et al. universal inference or prequential). Corpus-relative fitting reintroduces bias. Admissible dependent-variable merging (Vovk & Wang) is weighted average = stratified prior. **Conclusion: E-values collapse to stratified priors, not a third option.**

**Fable's response (via relay):** Conceded all three; reframed core output as **coalitional cost-sharing problem**: order-free set-valued total (the valuation function) + overlap graph (sparse interaction structure). Synthesis layer computes per-fact credit as presentation artifact under a named allocation scheme (Shapley, proportional, lexicographic). **Core emits only functions of (corpus + declared conventions) with zero undeclared degrees of freedom.**

**Theory outcome:** Three rounds of simulation-driven refinement converged on a cleaner model: order-free totals as ground truth, attribution as presentation, declared conventions as auditability criterion. No commits — theory/simulation work (Python scripts).

### normalize

Light continuation on VCS work from 2026-07-28.

### mirrorer

**Session d81af47a** — Light exploration or status work.

## Token Usage

| Project | Sessions | Output Tokens |
|---------|----------|---------------|
| fractal | 1+ | ~20,000+ |
| normalize | 1+ | ~2,000 |
| mirrorer, others | 1+ | ~1,500 |

**Total**: ~23,500 output tokens.

## Observations

1. **Theory work reached stable equilibrium.** After three rounds, the fact-attribution framework converged on coalitional cost-sharing (order-free totals + presentation-layer attribution). The conservation law held across all attempted unifications: complexity charges relocate, never vanish.
2. **Fractal's core redesign path clear.** Reframing core output (totals + overlap graph, per-fact credit as synthesis artifact) is a substantive architectural change, but it resolves the pathological order-dependence in a principled way.
3. **Light activity elsewhere.** Normalize and other projects showed steady maintenance rather than new initiatives, reflecting focus on fractal's theory work.

## Projects Touched

- **fractal** — information-theoretic fact attribution theory, convergence round 2.
- **normalize** — light continuation.
- **mirrorer** — light exploration.
