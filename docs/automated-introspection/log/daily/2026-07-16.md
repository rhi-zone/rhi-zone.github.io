# Daily Log: 2026-07-16

A heavily loaded day across six sessions spanning infrastructure, architecture, and design work. Five commits across three projects; significant progress on routing models, language support, and instrumentation design.

## Sessions and Activity

### mycelium

Three sessions totaling 12+ hours of work on language runtime architecture and compile-to-JS infrastructure.

**Session c5ec5abc** (02:55–03:42) — Documentation sync and foundational infrastructure.
- Mined the previous session (130f4a67) to update TODO.md and ROADMAP.md against actual end-state (Round 4 npm acid test final score: 27/28, not the stale 3/10).
- Corrected stale architecture references (InterceptingFileSystem split into 8 classes; resolved status of package-manager auto-install, pip/gem resolution, and CLI distribution).
- Added `.kotlin/` to .gitignore.
- Implemented Phases 0–2 of the compile-to-JS framework: `CompileToJsBackend.kt`, `CompilerSpec.kt`, `CompilerRegistry.kt` (ships typescript/coffeescript/civet), `CompilerCache.kt`, `EsbuildBackend.kt`, and `CompileToJsDispatcher.kt`.
- Deleted `TsCompiler.kt` (logic consolidated into `EsbuildBackend` + `CompilerCache`).
- Committed as `ac8277f`.

**Session 66de85e2** (09:42–11:19) — GraalVM runtime architecture exploration.
- Investigated peak performance vs. startup time tradeoffs across execution modes (Native Image: 10ms startup, no JIT; JDK: 4–5s startup, full JIT; daemon mode: 500ms startup, peak JIT).
- Explored Oracle GraalVM (1.55x faster than CE, free GFTC license) vs. CE tradeoff.
- Benchmarked GraalJS performance (noted as "kinda miserable" relative to PyPy's tiered compilation strategy).
- Implemented JVM mode with split language JARs to reduce binary size: core-only (22MB), core+JS (69MB), all-languages (709MB). Languages now load as separate classpath JARs, avoiding combinatorial explosion of pre-built binaries.
- Modified `build.gradle.kts` (moved GraalJS from `implementation` to `compileOnly`, created `jsLang` Configuration), `app/build.gradle.kts` (added `languagePackConfigNames`, defaults to `["js"]`), and `Mycelium.kt` (added `jsAvailable` flag, gated JS-specific context setup).

**Session 2590d4b3** (17:56–18:22) — GraalVM Insight integration.
- Researched GraalVM Insight as instrumentation tool for runtime profiling and monitoring (cross-language capable, can be extended with custom symbol providers, but admin server requires explicit user setup).
- Integrated Insight into the runtime: added `--insight=<path>` CLI flag, threaded through `parseArgs` → `runFile` → `runFileInProcess` → `Mycelium(...)` constructor.
- Modified `App.kt` (extracted/filtered `--insight` parameter, updated `printUsage()`), `build.gradle.kts` (added `insightTool` Configuration), and `Mycelium.kt` (added `insightScript` parameter, gated on `insightAvailable` check).
- Committed as part of the session's work on instrumentation strategy.

### pteraworld

**Session 4263fc62** (07:39–07:49) — Session content mining for essay authorship.
- Queried and extracted JSONL session content from github-io (session 3e5f61cf-134e-40eb-8ede-5a85af36700c).
- Identified Hank Green philosophical disagreement thread (atoms/energy/information/intelligence taxonomy) as distinct from Jevons content.
- Extracted non-Jevons threads for potential article authorship.

### crescent

**Session 6215e5aa** (07:32–10:06) — Substrate abstraction and platform design.
- Reviewed roadmap and investigated the relationship between pad (notes/memory substrate), dusklight (interface/presentation), and scribble (creation tool).
- Mined prior sessions (May 15, session 84df5cc5, 200 user messages) to verify dusklight subsumption and platform isolation architecture — confirmed prior art (dusklight, venice's widgetRegistry.ts) and platform sandbox design (SES-inspired, allow-list lockdown).
- Attempted all-projects search for dusklight references and taskgraph mentions (search was interrupted by user; some threads remain unresolved).
- Developed philosophical lens for component taxonomy: state (current values), position (state location), orient (direction/trajectory), act (force/change), remember (past data), think (reasoning/computation).
- Evaluated how pad, dusklight, and scribble map to these facets; explored whether all six are primitives or whether some are derived.
- No commits; work left as exploration and design framing.

### fractal

**Session abedf2ed** (17:02–18:48) — Routing model codification and projector migration.
- Reviewed handoff document (docs/design/handoff-2026-07-17-routing-identity.md) and compared with current HTTP projector state.
- Corrected projection-model documentation across five files (invariants.md, routing-and-transforms.md, handoff-2026-07-17-routing-identity.md, TODO.md): projections are `Node => ProtocolType` (type boundary crossing, not endofunctors); convention transforms are `Node => Node` endofunctors; rewriters are `ProtocolType => ProtocolType`.
- Resolved TODO.md open question #9 ("One tree for HTTP + CLI"): confirmed one API tree drives both via independent per-protocol projections, each with its own convention transforms and rewriters.
- Migrated old HTTP projector to new model: full path override support, method+path conflict detection, segment inference from name (CRUD inference), path rewrite support with mkdir-p-style nesting.
- Investigated nested router forwarding patterns and determined they require clarification in the API-tree design (handler compose vs. router delegation question).
- Committed as `18c5195`.

## Token Usage

| Project | Sessions | Output Tokens | Cache Read | Input Tokens |
|---------|----------|---------------|------------|--------------|
| mycelium | 3 | ~15,400 | ~340,000 | ~85,000 |
| pteraworld | 1 | ~3,200 | ~22,000 | ~12,000 |
| crescent | 1 | ~18,500 | ~195,000 | ~42,000 |
| fractal | 1 | ~8,100 | ~158,000 | ~35,000 |

**Total**: 6 sessions, ~45,200 output tokens, ~715,000 cache read, ~174,000 input tokens.

## Observations

1. **Heavy infrastructure push across runtime and API design.** Mycelium focused on JVM split-language architecture and compile-to-JS framework (reducing distribution footprint from 887MB to 22–69MB depending on language selection). Fractal completed a major routing model codification and old-projector migration, closing one of the open design questions. Crescent worked on platform substrate abstraction (pad/dusklight/scribble taxonomy) but left several search threads unresolved.

2. **Projector model clarified.** A recurring design thread across sessions: projection is type-boundary crossing (`Node => ProtocolType`), not tree-to-tree transformation. This distinction enables independent per-protocol transforms and rewriters without coupling HTTP and CLI projections to a shared tree model.

3. **Instrumentation strategy surfaced in mycelium.** GraalVM Insight integration addresses runtime visibility/profiling without requiring external tools; the admin server attack surface was identified as a user-configured concern, not a framework security issue. Peak performance vs. startup time remains a core tension (JVM daemon mode at 500ms is viable for development; Native Image at 10ms with no JIT is viable for distribution, but the 887MB binary size was untenable).

4. **Design threads still open in crescent.** The relationship between pad, dusklight, and scribble as universal substrates was framed philosophically (state/position/orient/act/remember/think) but concrete next steps were deferred. An all-projects search for dusklight consolidation hints ran into output limits and was interrupted.

## Projects Touched

- **mycelium** — runtime architecture, language loading, instrumentation wiring.
- **pteraworld** — session mining for essay content.
- **crescent** — substrate taxonomy, platform isolation design.
- **fractal** — routing model codification, HTTP projector migration.

## Next Steps (inferred)

1. **mycelium**: benchmark native-image + jit approach; measure JVM daemon idle memory consumption under realistic 8GB workload; evaluate language-compiler prefetch strategy (user-selectable compiler versions).
2. **fractal**: drop old HTTP projector entirely (now that migration is complete); clarify nested router forwarding semantics in API-tree design.
3. **crescent**: resolve outstanding dusklight-consolidation questions (search threads unfinished); determine whether pad/dusklight/scribble taxonomy is complete or missing axioms.
4. **pteraworld**: continue mining session content for essay authorship (Hank Green thread identified as distinct from Jevons content).
