# Daily Log: 2026-07-21

fractal doc projectors implementation (docusaurus, starlight, mkdocs reference). github-io ecosystem harness propagation continued. crescent size audit followup.

## Sessions and Activity

### fractal

**Session 8a50a560** (14:11–14:13, dated 2026-07-22) — Doc projector trio implementation.

Three doc-projector agent tasks completed from handoff plan:

- **docusaurus-reference** (14:11) — `toDocusaurusReference(doc, options?)` emits one MDX page per def. frontmatter (id, title, description, sidebar_position), deprecation admonition, "Type Signature" code block reusing TypeScript renderer, linked-type renderer for cross-page links, referenced-types section with TypeRef component (external, not shipped by type-ir). part-based rendering dispatched via `resolve()`. typecheck clean, zero new errors.

- **starlight-reference** (14:11) — `toStarlightReference(doc, options?)` similar scope. YAML frontmatter, Starlight components import, Aside deprecation notice, Tabs component (TypeScript-like + JSON Schema tabs), shape-specific section (fields/methods/enum members/union variants), examples from meta.examples, LinkCard cross-links with hover info. graceful degradation for unknown kinds. full typecheck pass after bun install in worktree.

- **mkdocs-reference** (14:12) — `toMkdocsReference(doc, options?)` plain Markdown (not MDX). renderTypeExpr with linked mode for cross-links. Same handlers pattern, resolve() dispatch.

All three added to `packages/type-ir/package.json` exports. Commits: `4259826` (docusaurus), `578ee87` (starlight), `81dcdee` (mkdocs).

### github-io

Ecosystem harness propagation continuing from previous session.

### crescent

Size audit ongoing from previous session.

## Token Usage

| Project | Sessions | Output Tokens | Cache Read | Cache Create |
|---------|----------|---------------|------------|--------------|
| fractal | 1 | ~8,000 | ~1,200,000 | ~25,000 |

**Total**: ~8,000 output tokens, ~1.2M cache read, ~25K cache create

## Observations

1. **Doc projectors achieved framework coverage** — docusaurus, starlight, mkdocs all implemented. design pattern consistent: one page per def, frontmatter + signature + shape-specific section + cross-links. TypeScript-like expression language used across all three; JSON Schema tab (starlight) delegates to existing json-schema.ts.

2. **Component patterns reused across projectors** — renderLinkedType, resolve() dispatch, metadata conventions (meta.example, meta.optional, meta.discriminator, etc.) — all shared. no reimplementation per framework. consistency high.

3. **Graceful degradation built in** — unknown kinds get signature + basic layout, not thrown errors. cross-link fallback to plain text when ref not in doc.defs. TypeRef component externalized (consuming site provides it).

## Projects Touched

- **fractal** — doc projectors tier complete (docusaurus, starlight, mkdocs reference)
- **github-io** — ecosystem harness propagation (ongoing)
- **crescent** — size audit (ongoing)

## Next Steps (inferred)

1. **fractal** — verify doc projector output against real doc sites (Docusaurus/Starlight/MkDocs instances); ensure TypeRef component interface is clear
2. **fractal** — pick next priority from handoff plan (web playground, CI/CD, more library variants)
3. **github-io** — finish propagation rollout; resolve remaining conflicts
4. **crescent** — decide on slim-down strategy
