# 2026-02-07 Session Log

**Focus:** Heavy compiler/IR optimization pass, primarily on Reincarnate TypeScript codegen.

## Session Activity

### Major Projects

**Reincarnate (Flash → TypeScript decompiler)**
- Dominated the day with 12+ concurrent sessions
- Focused on reducing gap between FFDec (reference decompiler) and our TypeScript output quality
- Intensive iterative debugging of generated code patterns

**hologram (Discord RP bot)**
- Minor work on RAG/embeddings threshold tuning and permissions

**normalize**
- Documentation/architecture overview work (brief)

**ooxml**
- WML feature mapping expansion (from prior session, some continuation checking)

### Reincarnate Work Patterns

The day was characterized by **problem-spotting → patch → move-to-proper-place** cycles:

1. **Scope lookup resolution** — eliminated `Flash_Scope.findPropStrict` calls systematically
   - Resolved class member lookups to `this`
   - Stripped scope lookups from `GetField`/`SetField`
   - Addressed unqualified globals (rand, game, outputText, etc.)

2. **Redundant assignment elimination** — thousands of `vN = vM;` assignments from SSA block arguments
   - Worked on merging block parameter assignments properly
   - Trailing merge assign position fixes

3. **Type inference** — became clear this was a blocker for further progress
   - `as boolean` casts on method calls despite return type info available
   - ~445 `:any` annotations in Flash output
   - Multi-typed locals (e.g., `race = 0.0` then `race = "human"`)
   - Decision: need constraint-based solver, not just forward dataflow

4. **Control flow patterns** — caught problematic emit patterns:
   - Double negation in loops: `if (!(!v55)) {`
   - Empty branches
   - Redundant conditional phis
   - Short-circuit operators (`||`/`&&`) lowered prematurely

5. **Local variable names** — AVM2 bytecode does provide this info; currently discarded by Flash frontend

### Architectural Concerns Surfaced

**"Bandaid" pattern:** Multiple sessions flagged that fixes were being added to the emitter when they should be AST-to-AST passes:
- Ternary detection (`try_ternary_assigns`)
- Math.max/min detection (`try_minmax`)
- Discussion: these should operate on Shape, not during TypeScript emission

**`flush_side_effecting_inlines` architecture:** questioned as "fundamentally wrong" — unclear if it's the right approach

**SSA lowering:** concern about losing important structure info. User asked "are we losing important info by lowering to SSA?" — suggests IR could be richer before this step.

**Optimization preset inconsistency:** redundant cast elimination disabled for `literal` preset but not `optimized` — flagged as weird configuration choice.

### Testing & Regeneration

- Continuous regeneration of `~/cc-project/` (Corruption of Champions SWF) for testing
- Comparing output to FFDec's decompilation as gold standard
- Found naming collision: parameter named `game` shadowing game singleton

### Commit Practices

Multiple reminders about committing work regularly. At least one session noted missing commits despite plan saying "commit per phase." User emphasized: "the issue here is that commits aren't happening, full stop. they need to happen."

## Observations

1. **Problem-driven not plan-driven:** sessions started from concrete emitted code that looked wrong, then traced back to root cause. Very reactive debugging style.

2. **Scope creep upward:** What started as "cleanup TypeScript output" evolved into discovering type inference was the real bottleneck.

3. **Quality bar high:** Constant comparison to FFDec. User seems to have high standards for decompilation output — mentions "we don't really have optimizations right now" vs. needing them for readable output.

4. **Parallel problem threads:** Type inference, control flow, local names, scope resolution — all real problems discovered independently, need coordinating.

5. **Architecture clarification needed:** Bandaid pattern and whether SSA is lowering too early suggest IR design may need rethinking before further optimization work.

---

**Code work summary:** 291 user messages across 44 sessions. Highly iterative, concrete debugging. No major new features, primarily output quality improvements and infrastructure cleanup.
