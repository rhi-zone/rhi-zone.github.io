# HANDOFF — design-it-twice / obligation-gap thread

This hands the design-it-twice / obligation-gap thread to a FRESH session. Work from the
certified record, not from any prior transcript — the source conversation is bloated and
re-importing it would re-poison the new context.

## Sources of truth (in order)

1. [`CONFIRMED-FACTS.md`](./CONFIRMED-FACTS.md) — the **CERTIFIED** sections are settled.
   The `## 2026-07-02` section is **PENDING USER CERTIFICATION** (not settled).

Read the record. Do not reconstruct from memory or transcript.

## FIRST TASK (gate)

Walk the PENDING `## 2026-07-02` section with the user and get each line **certified,
corrected, or struck**. You may FLAG suspected nuance errors, but you CANNOT certify on the
user's behalf — certification is the user's alone. Do NOT build on any pending item as
settled until the user blesses it: an uncertified item treated as fact poisons every
downstream loop.

## THEN — attempts for the user's blessing

Each of the following is an **attempt**, not a conclusion. Design each in a FRESH SUBAGENT
context off the certified footing — do not reason it out in the main session. Name each
attempt's downsides/tradeoffs; everything is a tradeoff.

1. **The OBLIGATION principle** — "a correctly-identified defect is an open item to resolve
   or hand off, never to excuse." Currently ABSENT from the control surface, and distinct
   from the earned-standing/confidence disposition: different axis (follow-through, not
   epistemic standing). Decide whether/how it belongs — a disposition line, vs completing
   the subagent surfacing-injection with "a surfaced gap is unresolved, don't reassure past
   it," vs other. Note it's subagent-side vs main-side, and that text may not bind
   (mitigation, not fix).

2. **The design-it-twice reframe** — it is a DISPOSITION (no approach is "the" way without
   explicit, unconditional user blessing), not a "spawn N / lenses" procedure. The
   skill-as-procedure is the wrong container — it's what let the idea force structure.
   Decide what, if anything, changes. Note the confidence disposition already covers
   "settled only with earned standing," so a design-specific version may be redundant.

3. **The directed propagated-section line** the user asked for: stress that subagents are
   cheaper AND cleaner than inferring in the bloated main session (which reprocesses
   100–500k tokens every turn), and to prefer a fresh subagent read over inferring
   in-session. Draft it as an attempt — it's bounded and stands alone.

## Honest constraints to carry (from the certified record)

- Text/disposition does not reliably bind these failures — mitigation, not fix.
- Context is append-only: no selective eviction; the only lever is a full lossy reset.
- Genuine independence needs a different prior (a different model, or the user) — so the
  independent adversary (refuter) is USER-triggered, not self-summoned.
